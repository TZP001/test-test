//--------------------------------------------
// Resource file for CATGSMUIBoundaryPanel class
// En_FR
//--------------------------------------------

PanelTitre="边界定义";
ComboTitre="拓展类型：";
BodyTitre="曲面边线：";
FromTitre="限制 1：";
ToTitre="限制 2：";
BoundaryCombo0="完整边界";
BoundaryCombo1="点连续";
BoundaryCombo2="切线连续";
BoundaryCombo3="无拓展";

FraOperandes.Type.LongHelp=
"指定选定的边线
绕曲面边界拓展的方式。
- 可以选择创建完整的曲面边界（选项：完整边界）。
- 可以根据点来限制拓展
（选项：点连续）：边界不会显示任何孔。
- 可以根据切线连续情况来限制拓展
（选项：切线连续）：边界不会显示任何孔，
并且它将是正则边界。
- 仅可以保留选定的边线（选项：无拓展）";

FraOperandes.CmbList.LongHelp=
"指定选定的边线绕曲面边界拓展的方式。
- 可以选择创建完整的曲面边界（选项：完整边界）。
- 可以根据点来限制拓展（选项：点连续）： 
边界不会显示任何孔。
- 可以根据切线连续情况来限制拓展
（选项：切线连续）：边界不会显示任何孔，
并且它将是正则边界。
- 仅可以保留选定的边线（选项：无拓展）";

FraOperandes.Body.LongHelp="指定选定的曲面边线。";
FraOperandes.FraBody.LongHelp="指定选定的曲面边线。";

FraOperandes.From.LongHelp="如果需要，可以指定拓展的第一限制元素。";
FraOperandes.FraToCut.LongHelp="如果需要，可以指定拓展的第一限制元素。";

FraOperandes.To.LongHelp="如果需要，可以指定拓展的第二限制元素。";
FraOperandes.FraCutting.LongHelp="如果需要，可以指定拓展的第二限制元素。";



CATGuiBoundaryDimension="选定对象的尺寸不正确。所需尺寸：1   所选尺寸： ";
CATGuiBoundaryIntersect="必须仅有一个相交。并且存在： ";
CATGuiBoundaryReRoute="Brep 展示已更改，请替换错误的边线";
CATGuiBoundaryReRouteImpossible="无法重设。请首先更新您的模型";
CATGuiBoundaryMauvaisePart="禁止选定对象处于被动模式";
CATGuiBoundarySelKO="不正确的选定对象。不是有效点或曲线。"; 


CATGuiBoundaryNomInit="初始";
CATGuiBoundaryNomFrom="限制 1";
CATGuiBoundaryNomTo="限制 2";
