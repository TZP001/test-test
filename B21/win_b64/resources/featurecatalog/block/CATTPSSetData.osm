// COPYRIGHT DASSAULT SYSTEMES 2005
// here follow basic block definition
// - internal use only
// - any modification to this file must be checked before deliveries
// 
block CATTPSSetData {
  int $allocate #offset(1)
  boolean $allocate #offset(1)
  specobject $allocate #offset(0)
  specobject $list #offset(1)
  external $allocate #offset(1)
  int `Norm` #offset(1)
  boolean `Current` #offset(1)
  specobject `Members` #offset(1) #list
  external `DrwCont` #offset(1)
}
