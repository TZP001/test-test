Usage = "错误用法\n
用法：CATJoystickDriver -f[frequency] -bh[BrokerHostName] -m[calibration matrix]\n
其中：\n
        -fxxx      ：以赫兹 (Hz) 为单位指定驱动程序频率（例如 -f30），默认值：-f20 (20 Hz)
        -bhxxxx    ：指定代理主机名（例如：-bhoslo）默认为：-bhlocalhost
        -m[x,x...]：指定矩阵校正操纵杆。此矩阵按行来读取。
                     默认：标识 (-m[1,0,0,0,1,0,0,0,1])


例如：\n\n          CATJoystickDriver -f20 -m[0,1,0,1,0,0,0,0,-1] -bhoslo\n";
