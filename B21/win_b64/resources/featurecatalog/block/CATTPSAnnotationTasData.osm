// COPYRIGHT DASSAULT SYSTEMES 2005
// here follow basic block definition
// - internal use only
// - any modification to this file must be checked before deliveries
// 
block CATTPSAnnotationTasData {
  double $allocate #offset(3)
  specobject $allocate #offset(1)
  component $allocate #offset(1)
  double `DirNx` #offset(1)
  double `DirNy` #offset(2)
  double `DirNz` #offset(3)
  specobject `ReferenceFrame` #offset(1)
  component `Distribution` #offset(1)
}
