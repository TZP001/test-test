Function="Macro avec arguments"; 
Restriction="Dans les macros avec arguments, la méthode CATIA. Select. SelectElement n'est pas supportée (sur UNIX et NT), \net la méthode InputBox n'est pas supportée sur UNIX";
