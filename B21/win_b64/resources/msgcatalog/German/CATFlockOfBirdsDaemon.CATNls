Usage = "
Verwendung: CATFlockOfBirdsDriver [-f frequenz] [-com nr_ser_anschluss] [-bps geschw_ser_anschluss] [-host broker_hostname] [-m gerätenummer transformationsmatrix] [-n anzahl_geräte] [-mouse gerätenummer]

  -f : Sendefrequenz für Ereignisse von CATFlockOfBirdsDriver an die V5-Sitzung.
       Ohne Angabe wird der Standardwert von 100 Hz verwendet.

  -com : Nummer des seriellen Anschlusses, an den Flock Of Birds angeschlossen ist.
         Ohne Angabe wird der Standardwert 1 verwendet (COM1 in Windows-Systemen).

  -bps : Geschwindigkeit des seriellen Anschlusses.
         Ohne Angabe versucht der Treiber, die Verbindung zu Flock Of Birds mit einer Geschwindigkeit von 115200 bps herzustellen.

  -host : Name des Hosts, auf dem CATVisVRBroker ausgeführt wird.
          Ohne Angabe wird der lokale Host verwendet.

  -m : Position und Ausrichtung, die von Flock Of Birds empfangen werden, werden vor dem Senden
       an die V5-Sitzung mit dieser Matrix multipliziert. Die Matrix muss in Zeilen mit dem folgenden Format definiert werden:
       -m gerätenummer [m11,m12,m13][m21,m22,m23][m31,m32,m33][m41,m42,m43]
       Ohne Angabe wird die Einheitsmatrix verwendet.
       Für jeden Tracker kann eine Matrix angegeben werden.

  -n : Anzahl der Flock Of Birds, die gleichzeitig ausgeführt werden.
       Ohne Angabe wird von einem Flock Of Birds ausgegangen.

  -mouse : Gerätenummer des Flock Of Birds, an den eine Ascension 6D-Maus angeschlossen ist.
           Ohne Angabe wird davon ausgegangen, dass keine 6D-Maus verwendet wird. Die Nummerierung der Geräte beginnt bei 1.

  -debug : Aktiviert die Flock Of Birds-Anzeige von Fehlernachrichten.";

Configuring = "Bird wird konfiguriert. Bitte warten... ";
ConfigFailed = "Bird-Konfiguration fehlgeschlagen.";
ConfigSuccess = "Bird-Konfiguration erfolgreich.";
BrokerDeclaration = "Bird-Treiber wurde für V5-Broker deklariert.";
