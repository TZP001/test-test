INFO_HELP="
CATDMUDistributor 将给定列表中的 DMU 数据（CATProduct、相关高速缓存数据等）
   从当前位置复制到远程位置或计算
   要复制的文件列表。\n
用法：\n
   检索帮助信息：
      CATDMUDistributor -h\n
   将当前位置的 DMU 数据复制到远程位置：
      CATDMUDistributor inputdir cachedir docdir [-s char]\n
   计算要复制的文件列表：
      CATDMUDistributor inputdir cachedir docdir -list file [-s char]\n
Windows 示例：\n
   CATDMUDistributor -h
   CATDMUDistributor c:\input e:\cache e:\document\n
Unix 示例：\n
   CATDMUDistributor -h
   CATDMUDistributor /u/input /v/cache /v/document\n
参数：\n
   -h            ：帮助。
   inputdir      ：定义包含列出文档的文件的目录。
                   这些文件包含的行不等。
                   每一行必须包含文档的标识符。
                   对于基于文件的文档，标识符是
                   文档的路径（路径末尾包含前面为“CATDLN://”的 DLName）。
                   对于基于 VPM 的文档，标识符为“COID COMPID CAENV CATAB”。
   cachedir      ：定义远程高速缓存目录。
   docdir        ：定义远程目录（将包含除了
                   高速缓存数据的所有文档）。
                   此目录可以是前面为“CATDLN://”的 DLName。
   -list file    ：将包含要复制的文档列表的输出文件。
                   每一行包含原始数据的路径和由空白
                   分隔的复制数据的路径。
   -s char       ：在输入或输出文件中定义用于
                   分隔行上字段的字符。
                   默认情况下使用空白字符。\n
诊断：\n
   可能的退出状态值有：\n
      0   成功完成。\n
      1   由于以下某种原因而失败：
          - 许可证不可用
          - 已取消激活高速缓存管理
          - 缺少参数
          - 参数无效
          - 选项后缺少参数
          - 缺少输入目录
          - 缺少高速缓存目录
          - 缺少文档目录
          - 目录无效
          - 无法打开输入文件
          - 无法写入输出文件
      2   处理错误。\n
      3   部分处理。\n
排除故障：\n
   退出状态 = 1：
      使用标准错误文件中的信息
      修改命令行或环境。\n
   退出状态 = 2：
      联系本地支持人员。\n
   退出状态 = 3：
      修复产生问题的文件：其名称包含在标准
      错误文件中。文件的交互使用可能会产生问题。
";
ERR_PROCESSING="错误：CATDMUDistributor 处理失败。";
ERR_MISSING_INPUT_DIR="错误：必须定义输入目录。";
ERR_MISSING_CACHE_DIR="错误：必须定义高速缓存目录。";
ERR_MISSING_DOCUMENT_DIR="错误：必须定义文档目录。";
ERR_COPY="错误：无法复制与以下文档对应的高速缓存数据：";
OK_COPY="确定：已复制与以下文档对应的高速缓存数据：";
NO_COPY="错误：没有与以下文档对应的高速缓存数据：";
START_BATCH="CATDMUDistributor 启动时间：";
STOP_BATCH="CATDMUDistributor 在此周期之后停止：";
