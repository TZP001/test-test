// =====================================================================
// FEATURES OF THE LIBRARY
// =====================================================================

// * Symbol *
SPPBeginRelation = "관계 시작";

// * Symbol *
SPPBeginRelationNP = "선행 관계 없이 관계 시작";

// * Symbol *
SPPCTRelation = "사이클타임 관계";

// * Symbol *
Activity = "요약 액티비티";
Activity.DefaultDescription = "설명 없음";

// * Symbol *
PhysicalActivity = "액티비티";

// * Symbol *
LogicalActivity = "논리적 액티비티";

// * Symbol *
ControlFlow = "제어 플로우";

// * Symbol *
ProductFlow = "프로덕트 플로우";

// * Symbol *
StartSU = "시작";

// * Symbol *
StopSU = "중지";


// =====================================================================
// PARAMETERS OF THE FEATURES OF THE LIBRARY
// =====================================================================

// * Symbol *
SBD = "시작 날짜";

// * Symbol *
SCT = "지정된 사이클타임";

// * Symbol *
CCT = "계산된 사이클타임";

// * Symbol *
newSpecifiedBeginning = "시작 날짜";

// * Symbol *
newSpecifiedCT = "지정된 사이클타임";

// * Symbol *
newCalculatedCT = "계산된 사이클타임";

// Relation Start-Start
SPPTimeConstraint_Start_Start = "시작-시작";

// Relation Start-Start
SPPTimeConstraint_End_End = "끝-끝";



