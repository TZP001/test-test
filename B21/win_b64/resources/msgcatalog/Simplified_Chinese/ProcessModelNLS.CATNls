// =====================================================================
// FEATURES OF THE LIBRARY
// =====================================================================

// * Symbol *
SPPBeginRelation = "开始关系";

// * Symbol *
SPPBeginRelationNP = "没有先行活动的初始关系";

// * Symbol *
SPPCTRelation = "周期时间关系";

// * Symbol *
Activity = "摘要活动";
Activity.DefaultDescription = "无描述";

// * Symbol *
PhysicalActivity = "活动";

// * Symbol *
LogicalActivity = "逻辑活动";

// * Symbol *
ControlFlow = "控制流";

// * Symbol *
ProductFlow = "产品流";

// * Symbol *
StartSU = "开始";

// * Symbol *
StopSU = "停止";


// =====================================================================
// PARAMETERS OF THE FEATURES OF THE LIBRARY
// =====================================================================

// * Symbol *
SBD = "开始日期";

// * Symbol *
SCT = "指定的周期时间";

// * Symbol *
CCT = "计算出的周期时间";

// * Symbol *
newSpecifiedBeginning = "开始日期";

// * Symbol *
newSpecifiedCT = "指定的周期时间";

// * Symbol *
newCalculatedCT = "计算出的周期时间";

// Relation Start-Start
SPPTimeConstraint_Start_Start = "开始 - 开始";

// Relation Start-Start
SPPTimeConstraint_End_End = "结束 - 结束";



