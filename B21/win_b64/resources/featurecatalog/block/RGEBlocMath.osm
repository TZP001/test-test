// COPYRIGHT DASSAULT SYSTEMES 2005
// here follow basic block definition
// - internal use only
// - any modification to this file must be checked before deliveries
// 
block RGEBlocMath {
  int $allocate #offset(1)
  double $allocate #offset(6)
  int `existe` #offset(1) #relative
  double `oX` #offset(1) #relative
  double `oY` #offset(2) #relative
  double `oZ` #offset(3) #relative
  double `vX` #offset(4) #relative
  double `vY` #offset(5) #relative
  double `vZ` #offset(6) #relative  
}
