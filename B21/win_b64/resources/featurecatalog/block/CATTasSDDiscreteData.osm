// COPYRIGHT DASSAULT SYSTEMES 2005
// here follow basic block definition
// - internal use only
// - any modification to this file must be checked before deliveries
// 
block CATTasSDDiscreteData {
  int $allocate #offset(1)
  int $list #offset(2)
  double $allocate #offset(0)
  double $list #offset(1)
  int `nValues` #offset(1)
  int `Frequencies` #offset(2) #list
  double `Values` #offset(1) #list
}
