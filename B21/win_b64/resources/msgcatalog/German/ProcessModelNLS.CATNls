// =====================================================================
// FEATURES OF THE LIBRARY
// =====================================================================

// * Symbol *
SPPBeginRelation = "Beziehung starten";

// * Symbol *
SPPBeginRelationNP = "Beziehung ohne Vorgänger starten";

// * Symbol *
SPPCTRelation = "Zykluszeitbeziehung";

// * Symbol *
Activity = "Abstrakte Aktivität";
Activity.DefaultDescription = "Keine Beschreibung";

// * Symbol *
PhysicalActivity = "Aktivität";

// * Symbol *
LogicalActivity = "Logische Aktivität";

// * Symbol *
ControlFlow = "Steuerungsfluss";

// * Symbol *
ProductFlow = "Produktfluss";

// * Symbol *
StartSU = "Start";

// * Symbol *
StopSU = "Stopp";


// =====================================================================
// PARAMETERS OF THE FEATURES OF THE LIBRARY
// =====================================================================

// * Symbol *
SBD = "Anfangsdatum";

// * Symbol *
SCT = "Angegebene Zykluszeit";

// * Symbol *
CCT = "Berechnete Zykluszeit";

// * Symbol *
newSpecifiedBeginning = "Anfangsdatum";

// * Symbol *
newSpecifiedCT = "Angegebene Zykluszeit";

// * Symbol *
newCalculatedCT = "Berechnete Zykluszeit";

// Relation Start-Start
SPPTimeConstraint_Start_Start = "Start-Start";

// Relation Start-Start
SPPTimeConstraint_End_End = "Ende-Ende";



