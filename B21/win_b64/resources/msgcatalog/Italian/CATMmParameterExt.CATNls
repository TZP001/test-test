RenameParameterMsg="Un parametro con il nome /P1 è già esistente. Si desidera assegnare al parametro questo nome in ogni caso? In caso negativo, gli verrà assegnato un nome standard";
RenameParameterHd="Assegnazione di un nuovo nome al parametro non completata";

