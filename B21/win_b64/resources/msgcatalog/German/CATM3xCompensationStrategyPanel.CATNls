Title = "Korrektur";

M3xOutputType			= "Korrekturausgabe";
M3xOutputType.LongHelp	= "Gibt an, wie die Anweisungen für die Korrektur des Fräsers
für die NC-Datenausgabe zu generieren sind:

Keine
Keine Berechnung der Korrektur

3D Kontakt
Die Korrektur wird entlang eines 3D-Vektors (XN, YN, ZN) berechnet, der senkrecht zur Teilefläche liegt und das Ende des Werkzeugs berührt.
Dabei wird der Eckenradius korrigiert.
Ausgabe sind der Kontaktpunkt (XC, YC, ZC) und die senkrechte Teilfläche (XN, YN, ZN).
Der Werkzeugspitze (XT, YT, ZT) kann ebenfalls angegeben werden, wenn diese Auswahl auf der Maschine festgelegt ist.
Die Ausgabe des Werkzeugachsenvektors (IJK) erfolgt im Mehrfachachsensystem.";
