CATPrtCreatePatternCommand.SelSupportState.Message="Die Komponente oder den zugeordneten Körper für das Muster auswählen.";
CATPrtCreatePatternCommand.ModState.Message="Die Richtungen auswählen und die erforderlichen Parameter angeben.";

WarningMsg="Die Liste der Komponenten, aus denen ein Muster erzeugt werden soll, enthält Elemente
mit nicht verschiebbaren Unterelementen (z. B. Drahtmodellgeometrie, Kanten usw.).
Die Musteroperation kann daher zu falschen Ergebnissen führen.
(Wenn ein Muster für eine Benutzerkomponente erzeugt werden soll, zuerst den Musterbefehl und
dann die Benutzerkomponente auswählen, damit stabile Ergebnisse entstehen.)
Soll die Operation fortgesetzt werden?";

UDFKeepSpecWarning="Die Option 'Spezifikationen beibehalten' kann nicht für das Muster der benutzerdefinierten Komponente verwendet werden.\nDiese Option muss zurückgesetzt werden.";


UDFWireFrameWarning="Die benutzerdefinierte Komponente, aus der ein Muster erzeugt werden soll, enthält nicht verschiebbare Unterelemente (z. B. Drahtmodellgeometrie, Kanten usw.).
Die Musteroperation kann daher zu falschen Ergebnissen führen.
Zuerst den Musterbefehl aufrufen und dann die Benutzerkomponente auswählen, damit stabile Ergebnisse erzeugt werden.
Soll die Operation fortgesetzt werden?";
