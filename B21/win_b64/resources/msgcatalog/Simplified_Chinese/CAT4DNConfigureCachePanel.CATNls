Title="配置";

AccessibleDirFrame.AccessibleDirLabel.Title="可用目录";

AccessibleDirFrame.AccessibleDirSelectorList.LongHelp=
"“可用”目录是指用户可访问，
用于“释放性高速缓存”的目录。
该列表由管理员定义，不能交互地修改。";

AddRemoveFrame.AddToCurrentPushB.ShortHelp="添加至当前目录";

AddRemoveFrame.DeletePushB.ShortHelp="从当前目录移除";

CurrentDirFrame.CurrentDirLabel.Title="当前目录";

CurrentDirFrame.CurrentDirSelectorList.LongHelp=
"只读高速缓存不一定在计算机上，
我们可以为释放性高速缓存定义多个目录。如果在本地高速缓存中
未找到网格化的文件，则按列出顺序浏览释放性目录，
检查该网格文件是否在释放性目录中。如果仍然没有找到该文件，
就网格化该文件并将其保存在本地高速缓存中";

CurrentDirFrame.OrderFrame.BrowsePushB.ShortHelp="选择路径";

CurrentDirFrame.OrderFrame.MoveUpPushB.ShortHelp="向上";
CurrentDirFrame.OrderFrame.MoveUpPushB.LongHelp="向上。
修改浏览已释放的目录的顺序。";

CurrentDirFrame.OrderFrame.MoveDownPushB.ShortHelp="向下";
CurrentDirFrame.OrderFrame.MoveDownPushB.LongHelp="向下。
修改浏览已释放的目录的顺序。";

DirectoryReleaseBrowser.Title="到释放性高速缓存的路径";

ErrorMessage0="未定义";
ErrorMessage1="未设置环境变量 AVAILABLE_CACHE_DIR_PATH。";
ErrorMessage2a="不能打开";
ErrorMessage2b=" 文件。";
ErrorMessage2c="由 AVAILABLE_CACHE_DIR_PATH 指向";

