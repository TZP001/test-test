// COPYRIGHT DASSAULT SYSTEMES 2005
// here follow basic block definition
// - internal use only
// - any modification to this file must be checked before deliveries
// 
block CATTPSShiftedProfData {
  int $allocate #offset(1)
  boolean $allocate #offset(1)
  component $allocate #offset(2)
  int `ProfTolValueType` #offset(1)
  component `ProfNumValue` #offset(1)
  component `ProfTabValue` #offset(2)
  boolean `ShiftedProf` #offset(1)
}
