Title="Konfiguration";

AccessibleDirFrame.AccessibleDirLabel.Title="Verfügbare Verzeichnisse";

AccessibleDirFrame.AccessibleDirSelectorList.LongHelp=
"Die verfügbaren Verzeichnisse stehen für den Benutzer zur Verfügung und
werden für den freigegebenen Cache verwendet.
Die Liste wird vom Administrator definiert und kann nicht interaktiv geändert werden.";

AddRemoveFrame.AddToCurrentPushB.ShortHelp="Den aktuellen Verzeichnissen hinzufügen";

AddRemoveFrame.DeletePushB.ShortHelp="Von aktuellen Verzeichnissen entfernen";

CurrentDirFrame.CurrentDirLabel.Title="Aktuelle Verzeichnisse";

CurrentDirFrame.CurrentDirSelectorList.LongHelp=
"Dies ist ein schreibgeschützter Cache, der sich nicht notwendigerweise auf der lokalen Maschine
befinden muss. Für den freigegebenen Cache können mehrere Verzeichnisse definiert werden.
Wird die tessellierte Datei im lokalen Cache nicht gefunden, werden die freigegebenen Verzeichnisse
in der angegebenen Reihenfolge nach dieser Datei durchsucht. Kann die Datei auch dort nicht
gefunden werden, wird die Komponente tesselliert und im lokalen Cache gesichert";

CurrentDirFrame.OrderFrame.BrowsePushB.ShortHelp="Einen Pfad auswählen";

CurrentDirFrame.OrderFrame.MoveUpPushB.ShortHelp="Aufwärts";
CurrentDirFrame.OrderFrame.MoveUpPushB.LongHelp="Aufwärts
Die Reihenfolge ändern, in der die freigegebenen Verzeichnisse durchsucht werden sollen.";

CurrentDirFrame.OrderFrame.MoveDownPushB.ShortHelp="Abwärts";
CurrentDirFrame.OrderFrame.MoveDownPushB.LongHelp="Abwärts
Die Reihenfolge ändern, in der die freigegebenen Verzeichnisse durchsucht werden sollen.";

DirectoryReleaseBrowser.Title="Pfad(e) zum freigegebenen Cache";

ErrorMessage0="Nicht definiert";
ErrorMessage1="Umgebungsvariable AVAILABLE_CACHE_DIR_PATH nicht gesetzt.";
ErrorMessage2a="Die Datei ";
ErrorMessage2b=" kann nicht geöffnet werden.";
ErrorMessage2c="auf die AVAILABLE_CACHE_DIR_PATH verweist";

