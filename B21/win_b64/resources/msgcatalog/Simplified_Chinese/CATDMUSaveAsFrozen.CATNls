INFO_HELP="
CATDMUSaveAsFrozen 可用来管理 DMU 相关文档（产品、几何图形和高速缓存数据）。\n
用法：\n
   检索帮助信息：
      CATDMUSaveAsFrozen -h\n
   将与产品相关的文档保存在目录中：
      CATDMUSaveAsFrozen -save directory -product productpath
                         [-prefix prefix][-data][-cache] 
                         [-user user -spwd cryppwd -server srv]\n
   在目录中导出与产品相关的文档：
      CATDMUSaveAsFrozen -export directory -product productpath
                         [-prefix prefix][-data]
                         [-user user -spwd cryppwd -server srv]
                         [-s char][-list listfile]\n
   在高速缓存中导入目录的内容：
      CATDMUSaveAsFrozen -import directory
                         [-s char][-list listfile]\n
Windows 示例：\n
   CATDMUSaveAsFrozen -h
   CATDMUSaveAsFrozen -import c:\u\input\n
Unix 示例：\n
   CATDMUSaveAsFrozen -h
   CATDMUSaveAsFrozen -export /u/export1 -product /u/sample.CATProduct
   CATDMUSaveAsFrozen -save /u/save2 -product /u/sample.CATProduct\n
参数：\n
   -h                   ：帮助。
   -save directory      ：将与产品相关的文档保存在
                          目录中。
                          此目录可以是前面为“CATDLN://”的 DLName。

   -export directory    ：在目录中导出与产品相关
                          的文档。
                          此目录可以是前面为“CATDLN://”的 DLName。
   -import directory    ：在高速缓存中导入目录的内容。
                          此目录可以是前面为“CATDLN://”的 DLName。
   -product productpath ：定义要处理的产品。
                          此路径可包含前面为“CATDLN://”的 DLName。
   -prefix prefix       ：保存名称中带有前缀的所有产品。
   -data                ：保存几何图形。
   -cache               ：保存高速缓存数据。
   -user user           ：定义 VPM 用户。
   -spwd cryppwd        ：定义用户的 VPM 密码。
                          使用前已解密。
   -server srv          ：定义 VPM 服务器。
   -s char              ：在输出文件中定义用于
                          分隔行上字段的字符。
   -list listfile       ：输出将包含要复制的文档列表
                          的文件。每一行包含原始数据的路径
                          和由分隔符字符分隔的
                          复制数据的路径（请参阅 -s 选项）。\n
   注意：若文档参考 VPM，则 user、spwd 和 server 参数为必需。\n
诊断：\n
   可能的退出状态值有：\n
      0   成功完成。\n
      1   由于以下某种原因而失败：
          - 许可证不可用
          - 已取消激活高速缓存管理
          - 缺少参数
          - 参数无效
          - 选项后缺少参数
          - 缺少输入文件
          - 无法找到文件
          - 无法打开输入文件
          - 文档类型错误\n
      2   处理错误。\n
      3   部分处理。\n
排除故障：\n
   退出状态 = 1：
      使用标准错误文件中的信息
      修改命令行或环境。\n
   退出状态 = 2：
      联系本地支持人员。\n
   退出状态 = 3：
      修复产生问题的文件：其名称包含在标准
      错误文件中。文件的交互使用可能会产生问题。
";
ERR_PROCESSING="错误：CATDMUSaveAsFrozen 处理失败。";
ERR_TREATMENT="错误：无法处理以下文档/目录：";
OK_TREATMENT="确定：已处理以下文档/目录：";
START_BATCH="CATDMUSaveAsFrozen 启动时间：";
STOP_BATCH="CATDMUSaveAsFrozen 在此周期之后停止：";
