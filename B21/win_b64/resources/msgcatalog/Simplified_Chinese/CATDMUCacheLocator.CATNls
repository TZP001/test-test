INFO_HELP="
CATDMUCacheLocator 定位与文档列表对应的高速缓存数据。\n
用法：\n
   检索帮助信息：
      CATDMUCacheLocator -h\n
   定位高速缓存数据：
      CATDMUCacheLocator inputfile outputfile [-s char] [-t type] [-p dir]\n
Windows 示例：\n
   CATDMUCacheLocator -h
   CATDMUCacheLocator c:\u\input.txt c:\u\output.txt
   CATDMUCacheLocator c:\u\input.txt c:\u\output.txt -p c:\cache -t 3dmap\n
Unix 示例：\n
   CATDMUCacheLocator -h
   CATDMUCacheLocator /u/input /u/output
   CATDMUCacheLocator /u/input /u/output -p /u/cache -t 3dmap\n
参数：\n
   -h            ：帮助。
   inputfile     ：包含文档列表的输入文件。
                   路径不能包含 DLName。
                   每一行必须包含文档标识符、
                   分隔符和文档时间戳。
                   对于基于文件的文档，标识符是
                   文档的路径（路径末尾包含前面为“CATDLN://”的 DLName）。
                   对于基于 VPM 的文档，标识符为“COID COMPID CAENV CATAB”。
                   时间戳使用 YYYY-mm-dd-HH.MM.SS 格式提供。
                   若文档存在，则可以省略时间戳。
   outputfile    ：将包含文档高速缓存位置列表的
                   输出文件。路径不能包含 DLName。
                   每一行将包含文档标识符、
                   分隔符和与文档对应的
                   高速缓存数据的位置。
   -s char       ：在输入或输出文件中定义用于
                   分隔行上字段的字符。
                   默认情况下使用空白字符。
   -t type       ：定义高速缓存类型（cgr、3dmap...），
                   默认情况下使用 cgr。
   -p dir        ：定义要使用的高速缓存目录，
                   而非设置的目录。在这种情况下，目录可以不存在
                   或不可访问，因此在列表中提供
                   文件。此目录不能为 DLName。\n
诊断：\n
   可能的退出状态值有：\n
      0   成功完成。\n
      1   由于以下某种原因而失败：
          - 许可证不可用
          - 已取消激活高速缓存管理
          - 缺少参数
          - 参数无效
          - 选项后缺少参数
          - 缺少输入文件
          - 无法找到文件
          - 无法打开输入文件\n
      2   处理错误。\n
排除故障：\n
   退出状态 = 1：
      使用标准错误文件中的信息
      修改命令行或环境。\n
   退出状态 = 2：
      联系本地支持人员。
";
ERR_PROCESSING="ERROR: CATDMUCacheLocator fails to process.";
START_BATCH="CATDMUCacheLocator starts at :";
STOP_BATCH="CATDMUCacheLocator stops after a duration of :";
